= simple_form_for(@contact, remote:true) do |f|
  = f.error_notification

  .form-inputs
    = f.input :first_name
    = f.input :last_name
    = f.input :phone

  .form-actions
    = f.button :submit
